on:
    schedule:
        - cron: '32 14 * * *''

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: Echo current time
              run: echo "Current time is $(date)"
